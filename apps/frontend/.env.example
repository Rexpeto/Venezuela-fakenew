# URL del backend (oRPC). Por defecto http://localhost:8787 si no se define.
PUBLIC_API_URL=http://localhost:8787
# "false" => usa la API real; cualquier otro valor => usa mocks.
PUBLIC_MOCK_API=false
