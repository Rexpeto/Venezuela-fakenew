# MCP server (Node/Bun) — copia a .env.
# Opcional: sin la key, verify_claim y search_official_sources usan solo el
# conocimiento base embebido (sin búsqueda web en tiempo real).
TAVILY_API_KEY=tvly-...
